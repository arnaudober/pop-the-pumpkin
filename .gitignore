# Godot-specific files
.import/         # Godot imports folder (contains the imported assets, not needed in the repository)
export/          # Godot export templates
*.godot.import   # Import state files (generated by Godot)
debug/           # Debug builds directory (if using custom builds)
*.godot           # Godot project configuration file (only if you have multiple environments and want to ignore the local project configuration)
.godot/*

# Operating system-specific files
.DS_Store        # macOS file system metadata
Thumbs.db        # Windows file system metadata
desktop.ini      # Windows desktop settings

# Editor-specific files (if using an editor other than Godot itself)
.vscode/          # VS Code settings and workspace files
.idea/            # JetBrains/IntelliJ IDEA project files